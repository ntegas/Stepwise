# Android Architecture (Expo-era) — SUPERSEDED

This document described offline sync, notifications, i18n, and units for a React Native/Expo build. **The platform decision has changed to native Android (Kotlin + Jetpack Compose)** — see `/ARCHITECTURE.md`, which covers all of these concerns in depth for the native stack (§8–9 offline/sync, §18 notifications, Technology Stack for i18n/units handling).

Kept in the repo for history — do not build against this file.
