export default function GoalsPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, marginBottom: 4 }}>Goals</h1>
      <p style={{ color: "#8a8b90", marginTop: 0 }}>
        Placeholder — Goal cards (cumulative/quantity/distance/financial/target/
        frequency/percentage) will render here, reading from the `goals` table.
      </p>
    </div>
  );
}
