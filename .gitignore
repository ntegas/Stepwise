node_modules/
.next/
out/
.env
.env.local
.DS_Store
*.log

# Gradle / Kotlin / Android build output — never committed, regenerated by CI/local builds.
.gradle/
build/
*/build/
**/build/
local.properties
.cxx/
captures/
*.apk
*.aab
*.ap_
*.dex

# Secret-bearing files (AI_CODE_SECURITY.md: excluded from AI context, not just from git).
# Extended now that real Gradle/Android files exist in this repo, per DEV-000's finding
# that this list was Next.js-only and hadn't yet been widened for the Android build.
*.pem
*.key
*.jks
*.keystore
google-services.json
key.properties

